import { PreResultViewPipe } from './pre-result-view.pipe';

describe('PreResultViewPipe', () => {
  it('create an instance', () => {
    const pipe = new PreResultViewPipe();
    expect(pipe).toBeTruthy();
  });
});
